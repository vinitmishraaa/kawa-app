// Mirrors the palette in tailwind.config.js — use this wherever a
// component needs a plain JS color value instead of a className
// (e.g. StatusBar, icon `color` props, map pins).
export const theme = {
  bark: "#2B2118",
  leaf: "#2F6B4F",
  leafLight: "#E7F0EA",
  clay: "#C96F4A",
  paper: "#F4F1E9",
  sand: "#EFE8D8",
  line: "#DDD3BE",
  ok: "#2F6B4F",
  warn: "#B8862E",
  danger: "#B3402F",
};
